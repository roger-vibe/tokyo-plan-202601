import { TripPlan, Language } from "../types";

// AI functionality disabled to reduce costs.
// This file acts as a placeholder for potential future re-integration.

export const generateTripPlan = async (language: Language): Promise<TripPlan> => {
  throw new Error("AI Trip Planning is disabled.");
};

export const generateLocationImage = async (locationName: string, description: string): Promise<string> => {
    throw new Error("AI Image Generation is disabled.");
};